<?php
// configuration parameters

@define('WAIT_EXEC_TIMEOUT',3,true);

// this commands is for example only, don't forget to replace its!

$rTorrentStartCmd = 'echo "Yo need to configure plugin starter!"';
$rTorrentStopCmd = 'echo "Yo need to configure plugin starter!"';
